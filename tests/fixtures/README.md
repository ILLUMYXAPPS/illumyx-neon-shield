# Controlled Match Fixtures

These fixtures are intentionally synthetic and cover exact, partial, single-signal, metadata-only, and false-positive cases. They are used to verify that scanner evidence survives the Review -> Decision -> Audit pipeline unchanged.
