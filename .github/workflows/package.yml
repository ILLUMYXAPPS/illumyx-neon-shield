name: Neon Shield Desktop Builds

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "app_ui.py"
      - "security_checks.py"
      - "NeonShield.spec"
      - "requirements-build.txt"
      - ".github/workflows/package.yml"
  push:
    branches: [main, "agent/**"]
    paths:
      - "app_ui.py"
      - "security_checks.py"
      - "NeonShield.spec"
      - "requirements-build.txt"
      - ".github/workflows/package.yml"

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            artifact: ILLUMYX-Neon-Shield-Windows
          - os: macos-latest
            artifact: ILLUMYX-Neon-Shield-macOS

    steps:
      - name: Check out repository
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.12"

      - name: Install build dependencies
        run: python -m pip install --upgrade pip -r requirements-build.txt

      - name: Compile source
        run: python -m compileall -q app_ui.py security_checks.py

      - name: Build desktop executable
        run: pyinstaller --clean --noconfirm NeonShield.spec

      - name: Upload packaged app
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: ${{ matrix.artifact }}
          path: dist/**
          if-no-files-found: error
