name: illumyx_neon_shield
version: 1.0.0-beta.1+1
publish_to: none
description: ILLUMYX Neon Shield mobile security posture dashboard.

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  device_info_plus: ^11.3.0
  network_info_plus: ^6.1.3
  package_info_plus: ^8.3.0
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
